\section{Results}\label{sec:results}

All central results use FRS 2024--25, period 2026, and \ProductionDraws\ assignments. Values after $\pm$ are standard deviations across artificial assignments, not standard errors or confidence intervals. Numerical Monte Carlo SE (SD divided by $\sqrt{\ProductionDraws}$) is stored separately in each JSON artifact. Five-assignment diagnostics are labelled exploratory.

\subsection{Derived sector shocks}\label{sec:results-shocks}

The deterministic full-schedule direct earnings stress is \pounds 0.886 billion annually; the EPD stress is \pounds 0.693 billion. These are scenario inputs generated by Equation~\eqref{eq:shock}, not estimated causal effects. The sector rates are exactly half those of the former $\varepsilon=2$ high case.

\subsection{Cushioning}\label{sec:results-cushioning}

\begin{table}[htbp]
\centering
\caption{Central direct-channel scenarios, \ProductionDraws\ assignments}
\label{tab:cushioning}
\small
\begin{tabular}{llrrrr}
\toprule
Schedule & Margin & Workers & Gross loss & Exchequer & Cushioning \\
& & (thousands) & (\pounds bn) & (\pounds bn) & (\%) \\
\midrule
Full & Displacement & \FullDisplacedWorkers & $\FullDisplacedGross\pm\FullDisplacedGrossSD$ & $\FullDisplacedExchequer\pm\FullDisplacedExchequerSD$ & $\FullDisplacedCushion\pm\FullDisplacedCushionSD$ \\
Full & Wage cut & --- & 0.886 & \FullWageExchequer & \FullWageCushion \\
Full & Inactivity & \FullDisplacedWorkers & $\FullDisplacedGross\pm\FullDisplacedGrossSD$ & $\FullInactiveExchequer\pm\FullInactiveExchequerSD$ & $\FullInactiveCushion\pm\FullInactiveCushionSD$ \\
EPD & Displacement & \EPDDisplacedWorkers & $\EPDDisplacedGross\pm\EPDDisplacedGrossSD$ & $\EPDDisplacedExchequer\pm\EPDDisplacedExchequerSD$ & $\EPDDisplacedCushion\pm\EPDDisplacedCushionSD$ \\
EPD & Wage cut & --- & 0.693 & \EPDWageExchequer & \EPDWageCushion \\
EPD & Inactivity & \EPDDisplacedWorkers & $\EPDDisplacedGross\pm\EPDDisplacedGrossSD$ & $\EPDInactiveExchequer\pm\EPDInactiveExchequerSD$ & $\EPDInactiveCushion\pm\EPDInactiveCushionSD$ \\
\bottomrule
\end{tabular}
\begin{minipage}{.94\textwidth}\footnotesize
\emph{Notes:} Worker, gross-loss and draw-based-margin values are means $\pm$ assignment SD. Wage-cut dispersion from the common UC claiming redraw is below the displayed precision. Earnings equivalence holds in expectation, not assignment by assignment.
\end{minipage}
\end{table}

The full-schedule displacement and wage-cut cushioning rates differ in their means, while displacement retains material assignment dispersion. For the displacement mean, the numerical Monte Carlo SE is \FullDisplacedCushionMCSE\ percentage points for cushioning and \pounds\FullDisplacedExchequerMCSE\ billion for Exchequer cost. These quantities measure simulation precision only. The decomposition remains economically informative: proportional cuts remove the top slice of earnings and therefore receive relief near marginal tax rates; displacement removes the whole earnings stream and receives relief nearer average rates, with UC providing part of the remainder. These are descriptive model mechanisms rather than estimates of behavioural insurance reliability.

\paragraph{Take-up sensitivity.} The regenerated grid applies the same post-shock-entitlement claiming rule to all margins. It uses only five assignments per cell and is exploratory. It shows that displacement responds more visibly to the assumed claiming probability, while wage-cut results move little because relatively few wage-cut households change UC entitlement. No grid difference is treated as statistically identified.

\subsection{Mechanism}\label{sec:results-mechanism}

The regenerated decomposition and gate audit are also five-assignment diagnostics. Income-tax relief is larger on the wage-cut margin than on displacement; UC is more important after displacement. The plain FRS input contains no usable household-capital measure, so the model cannot establish the empirical importance of UC's capital test. New-style JSA is not activated by the imposed transition. These omissions make the gate audit a description of the implemented model, not the full UK out-of-work system.

\subsection{Distributional incidence}\label{sec:results-margins}

Under full displacement, relative BHC poverty rises by $\FullDisplacedPoverty\pm\FullDisplacedPovertySD$ percentage points. Under the wage-cut scenario, relative BHC poverty is unchanged at displayed precision. The national poverty changes are economically small relative to the uncertainty omitted from the exercise and are not a headline estimate. They can coexist with large losses for selected households because the exposed population is limited and often above the poverty line before the shock.

\begin{figure}[htbp]
\centering
\includegraphics[width=.85\textwidth]{../results/figures/decile_by_margin_full_tariff.png}
\caption{Mean per-capita disposable-income change by baseline equivalised-income decile under the regenerated full-schedule scenarios. Shading shows assignment dispersion, not sampling uncertainty.}
\label{fig:decile-margins}
\end{figure}

\subsection{Fiscal incidence}\label{sec:results-fiscal}

\begin{table}[htbp]
\centering
\caption{Annual Exchequer cost (\pounds billion)}
\label{tab:fiscal}
\begin{tabular}{lccc}
\toprule
Schedule & Displacement & Wage cut & Inactivity \\
\midrule
Full & $\FullDisplacedExchequer\pm\FullDisplacedExchequerSD$ & \FullWageExchequer & $\FullInactiveExchequer\pm\FullInactiveExchequerSD$ \\
EPD & $\EPDDisplacedExchequer\pm\EPDDisplacedExchequerSD$ & \EPDWageExchequer & $\EPDInactiveExchequer\pm\EPDInactiveExchequerSD$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=.85\textwidth]{../results/figures/exchequer_cost_draws.png}
\caption{Annual Exchequer cost across regenerated assignments. The spread is assignment dispersion.}
\label{fig:exchequer-draws}
\end{figure}

\subsection{EPD counterfactual}\label{sec:results-epd}

On paired seeds, full minus EPD displacement differs by $\EPDWorkerDifference\pm\EPDWorkerDifferenceSD$ workers, \pounds $\EPDGrossDifference\pm\EPDGrossDifferenceSD$ billion of gross earnings, and \pounds $\EPDExchequerDifference\pm\EPDExchequerDifferenceSD$ billion of Exchequer cost. The poverty-rate difference is $\EPDPovertyDifference\pm\EPDPovertyDifferenceSD$ percentage points. These are paired scenario contrasts, not causal deal estimates.

\begin{figure}[htbp]
\centering
\includegraphics[width=.85\textwidth]{../results/figures/epd_counterfactual.png}
\caption{Regenerated full-schedule and EPD fiscal scenarios. Error bars are assignment SDs.}
\label{fig:epd}
\end{figure}
